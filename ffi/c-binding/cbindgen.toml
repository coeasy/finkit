language = "C"
include_guard = "FINKIT_H"
include_version = true
usize_is_size_t = true

[export]
# Test-only panic injection — not part of the stable ABI.
exclude = ["ta_ffi_panic_test"]

[fn]
sort_by = "Name"

[defines]
"target_os = windows" = "FINKIT_WINDOWS"

[parse]
parse_deps = false
