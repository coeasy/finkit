[package]
name = "finkit-python"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Python bindings for finkit"

[lints]
workspace = true

[lib]
crate-type = ["cdylib"]

[dependencies]
finkit = { path = "../../core", default-features = false, features = ["std", "serde", "indicators-all"] }
finkit-visualization = { path = "../../visualization", features = ["svg", "html"] }
pyo3 = { version = "0.23", features = ["extension-module"] }
numpy = "0.23"
ndarray = { workspace = true, optional = true }

[features]
default = ["formula"]
formula = ["finkit/formula", "dep:ndarray"]
abi3 = ["pyo3/abi3-py38"]