[package]
name = "finkit-ffi-common"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Shared FFI utilities, error mapping and indicator registry for Finkit language bindings"

[lints]
workspace = true

[dependencies]
finkit-factor-analysis = { path = "../../factor-analysis" }
serde = { workspace = true }
serde_json = { workspace = true }
