[package]
name = "finkit-runtime"
version.workspace = true
edition.workspace = true
authors.workspace = true
description = "Execution, graph, registry, and cache runtime for Finkit factors"
license.workspace = true
repository.workspace = true
homepage.workspace = true

[lints]
workspace = true

[dependencies]
finkit-array = { path = "../finkit-array" }
finkit-factor = { path = "../finkit-factor" }
finkit-series = { path = "../finkit-series" }
