# API Reference

Complete API documentation for the Rust core crate and all language bindings. Function signatures, parameters, return types, and error handling are documented for every public indicator and utility.

- English: `docs/api-reference.md`
- Chinese (中文): `docs/api-reference-zh.md`

Rust API docs are also generated via `cargo doc --workspace`.
