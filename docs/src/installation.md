# Installation

Install AlphaTA for your target language and platform. Python and Node.js packages can be installed via pip and npm; Java, Go, and .NET use their respective package managers. For building all bindings from a single command, see the one-click build scripts in the repository root.

Detailed per-language instructions, prerequisites, and troubleshooting are maintained in the repository at `docs/installation.md`.
