# AlphaTA Industrial-Grade Optimization

> **Branch:** feature/industrial-grade-upgrade | **Status:** completed | **Updated:** 2026-06-24

**Project Directory:** `E:\agent_learn\AlphaTA`

This project uses AzaLoop for autonomous development. Re-read this file at session start.

## Toolchain

| Action | Command |
|--------|---------|
| Build | `cargo build` |
| Test | `cargo test` |
| Lint | `cargo clippy -- -D warnings` |
| MCP Server | `npx @azaloop/mcp-server` |

## Judgment Boundaries

**ALWAYS**
- Follow `next_action` from every MCP tool response automatically
- Read `spec.md` before coding; update `plan.md` after each phase
- Analyze → Plan → Implement: outline approach in plan.md before writing code
- Pass `llm_verdict: { passed, reasoning }` to `aza_task_verify`
- Log errors to `plan.md` error table; write discoveries to `findings.md`
- Evidence first: run verification commands and show output before claiming success
- Run build + test before shipping (`aza_quality_check` → `aza_ship`)

**NEVER**
- Stop between stories (unless `done: true`)
- Claim completion without evidence — run tests/build and show results
- Skip verification — always call `aza_task_verify`
- Repeat exact same failing action — mutate approach on each retry

## Workflow

```
PRD:  aza_prd_generate → aza_prd_save → aza_prd_optimize [max 5 rounds] → aza_prd_check
Loop: aza_loop_next → spec.md → code → aza_task_verify → aza_task_complete → repeat
Done: aza_quality_check → aza_doc_generate → aza_ship
Fail: fix → re-verify (max 5) → aza_task_fail after 3 tries
```

## Spec Files (`.aza/specs/<story-id>/`)

| File | Purpose |
|------|---------|
| `spec.md` | Requirements + acceptance criteria (read before coding) |
| `plan.md` | Implementation plan + error log (update after each phase) |
| `findings.md` | Discoveries + references (write on any finding) |

## Key MCP Tools

| Tool | Purpose |
|------|---------|
| `aza_loop_next` | Get next task + coding prompt |
| `aza_task_verify` | Verify implementation (requires `llm_verdict`) |
| `aza_task_complete` | Mark task done |
| `aza_task_fail` | Record failure (engine decides retry/skip) |
| `aza_quality_check` | Full pipeline: lint → test → build → security |
| `aza_ship` | Build + test + git commit + PR |

## Tech Stack

- **language**: Rust

## Constraints

- 所有变更必须向后兼容现有批量 API
- cargo clippy --workspace --all-targets --all-features -- -D warnings 必须通过
- cargo test --workspace 必须通过
- 不得引入新的 unsafe 代码（SIMD/FFI 路径除外）
- 每个 Story 完成后更新相关文档

## Progress

**215**/276 done | **56** pending | **5** blocked

---
> Auto-generated by AzaLoop. Re-read at session start.
